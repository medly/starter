import { Button } from './Button.tsx';
import { Meta, Story, Props, Preview } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

<Meta title="Core" component={Button} />

# Button

Simple button component with different design & colors.

You can play with the props in canvas tab.

export const Template = args => <Button {...args}>CLICKME</Button>;

<Preview>
    <Story name="Button" args={{ variant: 'solid', onClick: action('Clicked') }}>
        {Template.bind({})}
    </Story>
</Preview>

<Props of={Button} />
